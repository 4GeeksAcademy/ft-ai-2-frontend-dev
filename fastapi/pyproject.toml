[project]
name = "fastapi-testing-demo"
version = "0.1.0"
description = "Minimal FastAPI testing demonstration"
readme = "README.md"
requires-python = ">=3.12"

[dependency-groups]
dev = ["pytest>=8.0.0"]

[tool.uv]
package = false