# Simple Agent Loop Architecture

## Tech Stack

- Python 3.12
- `uv` for python package management.
- `python-tcod` for scrollable chat history.
- `requests` for HTTP requests.
- LiteLLM for access to LLMs.
