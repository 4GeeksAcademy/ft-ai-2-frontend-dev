[project]
name = "brevity-analytics"
version = "0.1.0"
description = "Brevity.app analytics service (FastAPI + TinyDB)"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "pydantic>=2.0",
    "tinydb>=4.9.0",
    "uvicorn[standard]>=0.52.4",
]

[tool.uv]
package = false

