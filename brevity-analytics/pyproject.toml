[project]
name = "brevity-analytics"
version = "0.1.0"
description = "Brevity.app analytics service (FastAPI + TinyDB) — Session 0 stub"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "tinydb>=4.9.0",
    "uvicorn[standard]>=0.52.4",
]

[tool.uv]
package = false

