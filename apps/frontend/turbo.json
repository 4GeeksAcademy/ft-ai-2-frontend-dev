{
  "$schema": "https://turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "dev": {
      "command": ["next dev --port 3000"]
    },
    "build": {
      "command": ["next build"]
    },
    "lint": {
      "command": ["next lint"]
    }
  }
}